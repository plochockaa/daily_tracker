# Backend environment variables (optional - has defaults)
DATABASE_URL=sqlite:///./database.db
PORT=8000
# In production, set this to your deployed frontend URL (comma-separated for multiple)
ALLOWED_ORIGINS=http://localhost:3000
